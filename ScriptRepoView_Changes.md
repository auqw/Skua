# ScriptRepoView Changes

- Added script path display showing path from Scripts folder onward
- Fixed description text wrapping (removed MaxHeight constraint)
- Increased spacing between description and action buttons
- Made Load/Start buttons always visible (removed hover-only opacity)
- Fixed context menu bindings for Load/Start commands to use SelectedItem's commands
