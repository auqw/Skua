<Styles xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Design.PreviewWith>
        <Border Padding="20">
            <!-- Add Controls for Previewer Here -->
        </Border>
    </Design.PreviewWith>
    
    <Style Selector="TextBlock.titlebar-title">
        <Setter Property="Foreground" Value="{DynamicResource SkuaAccentBrush}" />
    </Style>

    <!-- TODO(style-scope): Replace Window.skua TextBlock with an opt-in class style (e.g. TextBlock.skua-text) and apply only where intended. -->
    <Style Selector="Window.skua TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource SkuaTextBrush}" />
    </Style>
</Styles>
